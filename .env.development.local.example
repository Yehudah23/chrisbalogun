# Example environment overrides for local development
# If VUE_APP_API_BASE_URL is empty (default), the frontend will send
# relative requests (e.g. /api/login) and the Vue dev-server proxy
# configured in `vue.config.js` will forward them to your backend.

# To point the frontend to a specific backend during development,
# set this to your machine IP, ngrok URL, or production API host.
# Examples:
# VUE_APP_API_BASE_URL=http://192.168.1.100:8000
# VUE_APP_API_BASE_URL=https://abcd1234.ngrok.io

# Leave empty to use dev-server proxy (recommended for local dev):
VUE_APP_API_BASE_URL=

# Notes:
# - After editing this file, restart the dev server to pick up changes.
# - To serve the frontend on your LAN so other devices can reach it:
#     npm run serve -- --host 0.0.0.0
# - To make the backend reachable from other devices, run Laravel with:
#     php artisan serve --host=0.0.0.0 --port=8000
